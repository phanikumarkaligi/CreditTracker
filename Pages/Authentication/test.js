let arr = [1, 2, 3, 4, 5];

function removeDuplicates(arr) {
  let result = [];
  arr.forEach((value, index, array) => {
    arr.indexOf;
  });
}
let res = [];
for (i = arr.length - 1; i > 0; i--) {
  res[i] = arr[arr.length - i];
}
